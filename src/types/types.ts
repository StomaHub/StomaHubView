export type Product = {
  id?: string;
  name: string;
  category: string;
  image: string;
  description: string;
  price: string;
  button: string;
};

 export type Doctor = {
    id:string;
    name: string,
    email: string,
    observation: string
}